# Perfil de CCS — datos extendidos

(Los datos esenciales viven eager en `CLAUDE.md`. Aquí lo que no se carga siempre.)

## Otros proyectos observados

En `~/projects/` de CCS, no explorados desde acá:
`life`, `rebuss`, `atmadharma`, `binocular`, `biografia`, `giftcards`, `kiddo_steps`, `minecart*`, `mhc`, `sumaclientes`, `timeline`, `yapp`.
