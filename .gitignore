# Claude Code internals
.claude/*
!.claude/settings.json
!.claude/settings.local.json
.claude.json

# Shell config
.bashrc
.bash_logout
.profile

# System
.local/
.config/
.cache/
.ssh/

# Transcripts en curso — solo los cerrados se versionan
transcripts/*-running.md

# Secretos sueltos (los PATs viven en ~/.secrets/gh/, las API keys en ~/.secrets/anthropic/, nunca en el repo)
*.key
*.token
*.apikey
